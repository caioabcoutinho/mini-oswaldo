<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="h3">Editar Hub</h1>
  <div>
    <%= link_to content_tag(:i, "", class: "bi bi-eye") + " Ver Detalhes", @hub, class: "btn btn-outline-secondary" %>
  </div>
</div>

<%= render "form", hub: @hub %>